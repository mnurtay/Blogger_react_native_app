export const MAIN_COLOR = '#1E90FF';
export const STATUSBAR_COLOR = '#000';

import { Dimensions } from 'react-native';
const windows = Dimensions.get('window');
export const h = windows.height;
export const w = windows.width;